# ios/project.yml
name: NewsSummarizer
options:
  bundleIdPrefix: com.tslsun
  deploymentTarget:
    iOS: "16.0"

packages:
  SwiftSoup:
    url: https://github.com/scinfu/SwiftSoup
    from: 2.7.2

targets:
  NewsSummarizer:
    type: application
    platform: iOS
    sources:
      - path: NewsSummarizer
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.tslsun.newssummarizer
        SWIFT_VERSION: "5.9"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    entitlements:
      path: NewsSummarizer/NewsSummarizer.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.tslsun.newssummarizer
    dependencies:
      - package: SwiftSoup
      - target: ShareExtension
        embed: true
    info:
      path: NewsSummarizer/Info.plist
      properties:
        UILaunchScreen: {}
        NSLocalNetworkUsageDescription: "Connects to Mac server over Tailscale to summarize articles"
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true

  ShareExtension:
    type: app-extension
    platform: iOS
    sources:
      - path: ShareExtension
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.tslsun.newssummarizer.shareextension
        SWIFT_VERSION: "5.9"
    entitlements:
      path: ShareExtension/ShareExtension.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.tslsun.newssummarizer
    dependencies:
      - package: SwiftSoup
    info:
      path: ShareExtension/Info.plist
      properties:
        CFBundleDisplayName: NewsSummarizer
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
        NSExtension:
          NSExtensionPointIdentifier: com.apple.share-services
          NSExtensionPrincipalClass: $(PRODUCT_MODULE_NAME).ShareViewController
          NSExtensionActivationRule: TRUEPREDICATE

  NewsSummarizerTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: NewsSummarizerTests
    settings:
      base:
        SWIFT_VERSION: "5.9"
        GENERATE_INFOPLIST_FILE: YES
    dependencies:
      - target: NewsSummarizer  # Shared/ types accessible via @testable import NewsSummarizer
